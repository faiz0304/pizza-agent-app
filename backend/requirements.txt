# ========================================================================
# PYTHON VERSION: 3.12.x Compatible
# ========================================================================
# 
# This requirements.txt is optimized for Python 3.12 with prebuilt wheels
# All packages have been tested to install without Rust/Cargo compilation
#
# INSTALLATION NOTE (Windows):
#   Use INSTALL-BACKEND-DEPS.bat which forces binary-only installation
#   Manual install: pip install numpy==1.26.4 --only-binary=numpy
#
# If experiencing build errors, clear pip cache first:
#   pip cache purge
#
# Last updated: 2026-01-03
# ========================================================================

# Core Dependencies - Install numpy first to avoid conflicts
# NumPy 1.26.4 has prebuilt wheels for Python 3.12 (cp312-win_amd64)
numpy==1.26.4

# FastAPI & Server
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.9

# Pydantic V2 (Full Python 3.12 support with cp312 wheels)
pydantic==2.10.3
pydantic-settings==2.6.1

# Database
pymongo==4.10.1
motor==3.6.0

# Vector Store & Embeddings (Python 3.12 compatible versions)
chromadb==0.5.23
sentence-transformers==3.3.1

# LLM Integration
huggingface-hub==0.26.5
requests==2.32.3

# Twilio (WhatsApp)
twilio==9.4.0

# Utilities
python-dotenv==1.0.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1

# Optional (for Groq & Google)
groq==0.11.0
google-generativeai==0.8.3
