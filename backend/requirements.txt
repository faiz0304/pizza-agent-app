# ========================================================================
# PYTHON VERSION: 3.10.x Required
# ========================================================================
# 
# This project requires Python 3.10 for stable Windows binary wheel support
# All packages have been tested with Python 3.10 (no compilation required)
#
# INSTALLATION:
#   1. Install Python 3.10.11: https://www.python.org/downloads/release/python-31011/
#   2. Run: REBUILD-BACKEND-VENV.bat
#   3. This creates fresh venv with Python 3.10 and installs all dependencies
#
# WHY PYTHON 3.10?:
#   - All dependencies have stable binary wheels for Python 3.10 on Windows
#   - numpy, faiss-cpu, pydantic, fastapi fully compatible
#   - No Rust/GCC/source compilation required
#   - Industry-standard version with long-term support
#
# Last updated: 2026-01-03
# ========================================================================

# Core Dependencies - Install numpy first to avoid conflicts
# NumPy 1.26.x series has prebuilt wheels for Python 3.12 (cp312-win_amd64)
# Using version range to let pip find best available wheel
numpy>=1.26,<2.0

# FastAPI & Server
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.9

# Pydantic V2 (Full Python 3.12 support with cp312 wheels)
pydantic==2.10.3
pydantic-settings==2.6.1

# Database
# motor 3.6.0 requires pymongo >=4.9,<4.10
pymongo==4.9.2
motor==3.6.0

# Vector Store & Embeddings (Python 3.12 compatible versions)
faiss-cpu==1.9.0
sentence-transformers==3.3.1

# LLM Integration
huggingface-hub==0.26.5
requests==2.32.3

# Twilio (WhatsApp)
twilio==9.4.0

# Utilities
python-dotenv==1.0.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1

# Optional (for Groq & Google)
groq==0.11.0
google-generativeai==0.8.3
